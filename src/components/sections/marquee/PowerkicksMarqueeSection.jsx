import TextTicker from "../../animations/TextTicker.jsx";

export default function PowerkicksMarqueeSection() {
    return (
        <section className="w-full py-[2rem] bg-black text-white -mt-px -mb-px overflow-hidden overflow-x-clip">
            <TextTicker>
                Powerkicks
            </TextTicker>
        </section>
    );
}